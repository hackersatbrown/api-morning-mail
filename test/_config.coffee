module.exports =
  # If true, use Redis as the key store when running the key API tests. (This
  # requires that you have a local Redis server running.) If false, use the
  # mock store.
  TEST_REDIS: false
